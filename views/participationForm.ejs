<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>प्रमाण पत्र डाउनलोड करें | सरस्वती पूजा 2026</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Devanagari:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --bg: #fdfaf3;
      --card: rgba(255, 255, 255, 0.75);
      --border: rgba(184, 134, 11, 0.25);
      --accent: #b8860b;
      --accent-dark: #8b5e00;
      --text: #2d2d2d;
      --text-light: #555;
      --success: #10b981;
      --error: #ef4444;
      --shadow-sm: 0 4px 15px rgba(0,0,0,0.06);
      --shadow-md: 0 10px 35px rgba(184,134,11,0.12);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      min-height: 100dvh;
      font-family: 'Noto Sans Devanagari', 'Inter', system-ui, sans-serif;
      background: linear-gradient(145deg, var(--bg), #4d27a7);
      display: grid;
      place-items: center;
      padding: 1.5rem;
      color: var(--text);
    }

    .container {
      width: 100%;
      max-width: 480px;
      background: var(--card);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border-radius: 1.5rem;
      border: 1px solid var(--border);
      box-shadow: var(--shadow-md);
      padding: 2.5rem 2rem;
      position: relative;
      overflow: hidden;
    }

    .container::before {
      content: "";
      position: absolute;
      inset: -50%;
      background: radial-gradient(circle at 30% 20%, rgba(184,134,11,0.08), transparent 40%);
      pointer-events: none;
    }

    .header {
      text-align: center;
      margin-bottom: 2.5rem;
    }

    .header h1 {
      font-size: 1.85rem;
      font-weight: 700;
      color: var(--accent-dark);
      margin-bottom: 0.5rem;
      letter-spacing: -0.02em;
    }

    .header p {
      color: var(--text-light);
      font-size: 1rem;
      line-height: 1.5;
    }

    .form-group {
      position: relative;
      margin-bottom: 2rem;
    }

    .floating-label {
      position: absolute;
      top: 1.1rem;
      left: 1.25rem;
      color: var(--text-light);
      font-size: 1rem;
      pointer-events: none;
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    }

    input {
      width: 100%;
      padding: 1.25rem 1.25rem 0.75rem;
      font-size: 1.125rem;
      font-family: 'Inter', monospace;
      letter-spacing: 2px;
      background: rgba(255,255,255,0.5);
      border: 2px solid var(--border);
      border-radius: 0.9rem;
      color: var(--text);
      transition: all 0.25s ease;
    }

    input:focus {
      outline: none;
      border-color: var(--accent);
      box-shadow: 0 0 0 4px rgba(184,134,11,0.12);
    }

    input:not(:placeholder-shown) + .floating-label,
    input:focus + .floating-label {
      top: 0.55rem;
      font-size: 0.82rem;
      color: var(--accent-dark);
      font-weight: 600;
    }

    .status-message {
      position: absolute;
      right: 1.2rem;
      top: 1.1rem;
      font-size: 1.1rem;
      transition: opacity 0.3s;
      opacity: 0;
      pointer-events: none;
    }

    .status-message.show {
      opacity: 1;
    }

    .status-success { color: var(--success); }
    .status-error   { color: var(--error);   }

    .note {
      font-size: 0.875rem;
      color: var(--text-light);
      margin-top: 0.75rem;
      line-height: 1.5;
      padding-left: 0.5rem;
    }

    .btn-submit {
      width: 100%;
      padding: 1.15rem;
      font-size: 1.1rem;
      font-weight: 600;
      color: white;
      background: linear-gradient(90deg, var(--accent), #d4a017);
      border: none;
      border-radius: 0.9rem;
      cursor: pointer;
      box-shadow: 0 6px 20px rgba(184,134,11,0.25);
      transition: all 0.28s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .btn-submit:hover:not(:disabled) {
      transform: translateY(-2px);
      box-shadow: 0 12px 30px rgba(184,134,11,0.35);
    }

    .btn-submit:disabled {
      background: #d1d5db;
      cursor: not-allowed;
      box-shadow: none;
    }

    .btn-spinner {
      display: inline-block;
      width: 1.1em;
      height: 1.1em;
      border: 2.5px solid white;
      border-radius: 50%;
      border-top-color: transparent;
      animation: spin 0.9s linear infinite;
      margin-right: 0.6rem;
      vertical-align: middle;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    .privacy {
      text-align: center;
      margin-top: 2rem;
      font-size: 0.875rem;
      color: var(--text-light);
    }

    .privacy a {
      color: var(--accent-dark);
      text-decoration: none;
      font-weight: 500;
    }

    .privacy a:hover {
      text-decoration: underline;
    }

    @media (max-width: 480px) {
      .container {
        padding: 2rem 1.5rem;
      }
      .header h1 {
        font-size: 1.65rem;
      }
    }
    .error-box {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  background: rgba(239, 68, 68, 0.08);
  border: 1.5px solid rgba(239, 68, 68, 0.35);
  color: var(--error);
  padding: 0.9rem 1.1rem;
  border-radius: 0.85rem;
  margin-bottom: 1.8rem;
  font-size: 0.95rem;
  font-weight: 500;
  animation: shake 0.35s ease-in-out;
}

.error-icon {
  font-size: 1.2rem;
}

@keyframes shake {
  0%   { transform: translateX(0); }
  25%  { transform: translateX(-4px); }
  50%  { transform: translateX(4px); }
  75%  { transform: translateX(-4px); }
  100% { transform: translateX(0); }
}

  </style>
</head>
<body>

  <div class="container">
    <div class="header">
      <h1>प्रमाण पत्र डाउनलोड करें</h1>
      <p>सरस्वती पूजा प्रतियोगिता – 2026<br>Ward-12, सरस्वती शिशु समाज, हरहंजा</p>
    </div>

    <form method="POST" action="/register/Participation" id="certForm" novalidate>
      <div class="form-group">
        <input
          type="text"
          id="aadharNumber"
          name="aadharNumber"
          required
          maxlength="12"
          pattern="[0-9]{12}"
          inputmode="numeric"
          autocomplete="off"
          placeholder=" "
          aria-describedby="aadharHelp"
        />
        <label for="aadharNumber" class="floating-label">आधार नंबर (12 अंक)</label>
        <span id="status" class="status-message" aria-live="polite"></span>
      </div>

      <p class="note" id="aadharHelp">
        हम आपकी जानकारी को अत्यंत सुरक्षित रखते हैं। यह केवल प्रमाण-पत्र जनरेट करने के लिए उपयोग होगी।
      </p>

      <button type="submit" class="btn-submit" id="submitBtn">
        प्रमाण पत्र डाउनलोड करें (PDF)
      </button>
    </form>

    <div class="privacy">
      आपका डेटा सुरक्षित है • <a href="#">Privacy Policy</a>
    </div>
  </div>
  <% if (typeof error !== "undefined" && error) { %>
  <div class="error-box" role="alert">
    <span class="error-icon">⚠️</span>
    <span class="error-text"><%= error %></span>
  </div>
<% } %>

  <script>
    const form  = document.getElementById('certForm');
    const input = document.getElementById('aadharNumber');
    const btn   = document.getElementById('submitBtn');
    const statusEl = document.getElementById('status');

    function updateStatus(length) {
      statusEl.className = 'status-message';
      statusEl.textContent = '';
      statusEl.classList.remove('show');

      if (length === 0) return;

      if (length < 12) {
        statusEl.textContent = '⬜';
        statusEl.classList.add('show');
      } else if (length === 12) {
        statusEl.textContent = '✅';
        statusEl.classList.add('status-success', 'show');
      }
    }

    input.addEventListener('input', e => {
      e.target.value = e.target.value.replace(/\D/g, '').slice(0,12);
      updateStatus(e.target.value.length);
      btn.disabled = e.target.value.length !== 12;
    });

    form.addEventListener('submit', e => {
      if (input.value.length !== 12) {
        e.preventDefault();
        alert('कृपया 12 अंकों का पूरा आधार नंबर दर्ज करें।');
        input.focus();
        return;
      }

      // Visual feedback
      btn.disabled = true;
      btn.innerHTML = '<span class="btn-spinner"></span> तैयार हो रहा है...';
    });

    // Initial status
    updateStatus(input.value.length);
    input.addEventListener('input', () => {
  const errorBox = document.querySelector('.error-box');
  if (errorBox) errorBox.remove();
});

  </script>

</body>
</html>